build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
.vscode/
.cursor/
.cache/
.DS_Store

# Secure Boot v2 signing key — must NEVER be committed (see SECURE_BOOT.md).
secure_boot_signing_key.pem
*.pem
